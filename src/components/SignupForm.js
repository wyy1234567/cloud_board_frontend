import React from 'react'
import { register } from '../requests'

export default class SignupForm extends React.Component {
    render() {
        return (
            <h4>I'm Signup Form Component!</h4>
        )
    }
}